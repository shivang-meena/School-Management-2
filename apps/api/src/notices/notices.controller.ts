import { Body, Controller, Get, Post, Req, UseGuards } from '@nestjs/common'; import { Role } from '@prisma/client'; import { CreateNoticeSchema } from '@erp/contracts'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { Roles } from '../auth/roles.decorator'; import { RolesGuard } from '../auth/roles.guard'; import { NoticesService } from './notices.service'; @Controller('notices') export class NoticesController { constructor(private readonly service: NoticesService) {} @Get('public') publicList() { return this.service.list(); } @Get() @UseGuards(JwtAuthGuard) list(@Req() req: any) { return this.service.list(req.user); } @Post() @UseGuards(JwtAuthGuard, RolesGuard) @Roles(Role.ADMIN) create(@Body() b: unknown, @Req() req: any) { return this.service.create(CreateNoticeSchema.parse(b), req.user.id); } }
