import { BadRequestException, ForbiddenException, Injectable, NotFoundException } from '@nestjs/common'; import { ResultEntryStatus, Role } from '@prisma/client'; import { CreateExamInput, EnterMarksInput } from '@erp/contracts'; import { PrismaService } from '../prisma/prisma.service';
@Injectable() export class ExamsService { constructor(private readonly prisma: PrismaService) {}
  create(dto: CreateExamInput) { if (dto.passMarks > dto.maximumMarks) throw new BadRequestException('Pass marks cannot exceed maximum marks'); return this.prisma.assessment.create({ data: { ...dto, date: new Date(dto.date), startTime: dto.startTime ? new Date(`1970-01-01T${dto.startTime}:00Z`) : null } }); }
  async enterMarks(dto: EnterMarksInput) { const assessment = await this.prisma.assessment.findUnique({ where: { id: dto.assessmentId } }); if (!assessment) throw new NotFoundException('Assessment not found'); if (assessment.published) throw new BadRequestException('Unpublish with reason before editing'); return this.prisma.$transaction(dto.results.map((row) => { if (!row.absent && row.marks == null) throw new BadRequestException('Marks or absent status is required'); if (row.marks != null && row.marks > Number(assessment.maximumMarks)) throw new BadRequestException('Marks exceed maximum'); return this.prisma.examResult.upsert({ where: { assessmentId_studentId: { assessmentId: dto.assessmentId, studentId: row.studentId } }, update: { entryStatus: row.absent ? ResultEntryStatus.ABSENT : ResultEntryStatus.MARKS_ENTERED, marks: row.absent ? null : row.marks, remarks: row.remarks }, create: { assessmentId: dto.assessmentId, studentId: row.studentId, entryStatus: row.absent ? ResultEntryStatus.ABSENT : ResultEntryStatus.MARKS_ENTERED, marks: row.absent ? null : row.marks, remarks: row.remarks } }); })); }
  async publish(id: string) { const assessment = await this.prisma.assessment.findUnique({ where: { id }, include: { results: true, section: { include: { enrollments: { where: { status: 'CURRENT' } } } }, gradingScheme: true } }); if (!assessment) throw new NotFoundException('Assessment not found'); if (assessment.results.length !== assessment.section.enrollments.length || assessment.results.some((r) => r.entryStatus === ResultEntryStatus.NOT_ENTERED)) throw new BadRequestException('All eligible student results must be resolved'); return this.prisma.assessment.update({ where: { id }, data: { published: true, publishedAt: new Date(), gradingSnapshot: assessment.gradingScheme?.bands || {} } }); }
  async list(actor: any, query: any) { const where: any = { academicYearId: query.academicYearId || undefined }; if (actor.role !== Role.ADMIN) where.published = true; if (actor.role === Role.STUDENT) where.results = { some: { student: { studentId: actor.studentId } } }; if (actor.role === Role.EMPLOYEE) { const assignments = await this.prisma.teacherAssignment.findMany({ where: { employeeId: actor.employeeDbId }, select: { sectionId: true } }); where.sectionId = { in: assignments.map((a) => a.sectionId) }; } return this.prisma.assessment.findMany({ where, include: { subject: true, section: { include: { schoolClass: true } }, results: actor.role === Role.STUDENT ? { where: { student: { studentId: actor.studentId } } } : true }, orderBy: { date: 'desc' } }); }
}
