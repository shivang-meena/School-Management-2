import { Body, Controller, Get, Post, Query, Req, UseGuards } from '@nestjs/common'; import { Role } from '@prisma/client'; import { CreateTransactionSchema } from '@erp/contracts'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { Roles } from '../auth/roles.decorator'; import { RolesGuard } from '../auth/roles.guard'; import { AccountsService } from './accounts.service'; @Controller('accounts') @UseGuards(JwtAuthGuard, RolesGuard) @Roles(Role.ADMIN) export class AccountsController { constructor(private readonly service: AccountsService) {} @Get() list(@Query() q: any) { return this.service.list(q); } @Get('summary') summary(@Query() q: any) { return this.service.summary(q); } @Post() create(@Body() b: unknown, @Req() req: any) { return this.service.create(CreateTransactionSchema.parse(b), req.user.id); } }
