import React from 'react'; import { useAuth } from '../../src/hooks/useAuth'; import { PortalModuleScreen } from '../../src/components/PortalModuleScreen'; export default function Screen(){const {user}=useAuth();return <PortalModuleScreen title="My Attendance" eyebrow="MARKED DAYS ONLY" description="Present, late, absent and leave records. Unmarked school days never appear as automatic absences." endpoint={`/attendance/students/${user?.studentId}`}/>}
